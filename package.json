{
  "name": "@mgwalker/amtrak-api",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "prettier": "^3.0.3"
  },
  "prettier": {},
  "dependencies": {
    "dayjs": "^1.11.10",
    "dotenv": "^16.3.1",
    "handlebars": "^4.7.8"
  },
  "scripts": {
    "site": "npm run update && node src/site/build.js",
    "update": "rm -rf _site && mkdir -p _site && node src/data/main.js"
  }
}
